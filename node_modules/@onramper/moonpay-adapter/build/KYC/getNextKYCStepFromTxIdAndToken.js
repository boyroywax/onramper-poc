"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const dynamoTxs_1 = require("./dynamoTxs");
const api_1 = require("./api");
const getNextKYCStep_1 = __importDefault(require("./getNextKYCStep"));
function default_1(id, token, onramperApiKey) {
    return __awaiter(this, void 0, void 0, function* () {
        const creationTx = yield dynamoTxs_1.getCreationTx(id);
        // TODO: In most of the times where this is called,
        // the currency has already been set, so we could just store
        // the customer data in a previous call and pull it from dynamo here
        // thus making everything faster by removing a network call
        const customerData = yield api_1.setFiatCurrency(token, creationTx.fiatCurrency);
        return getNextKYCStep_1.default(creationTx, token, customerData, onramperApiKey);
    });
}
exports.default = default_1;
