import { nextStep, dateInfo } from "../utils/types";
export declare function generateDate({ year, month, day }: dateInfo): string;
export default function (id: string, onramperApiKey: string, firstName: string, lastName: string, dateOfBirth: dateInfo, street: string, town: string, postCode: string, state: string | undefined, providedCountry: string): Promise<nextStep>;
