import { stepDataItems } from "../types";
import { encodeToken, decodedTokenType, decodeToken } from "./encodeToken";
export { encodeToken, decodeToken };
export declare function encodeJson(data: any): string;
export declare function randomId(): string;
export declare function checkTokenTypes<expectedTypes extends decodedTokenType>(decodedValues: decodedTokenType, sampleValues: expectedTypes): decodedValues is expectedTypes;
export declare function checkBodyParams(body: any, keys: stepDataItems): void;
