import { nextStep } from "./utils/types";
declare type BankInfo = {
    currencyCode: "eur";
    iban: string;
} | {
    currencyCode: "gbp";
    accountNumber: string;
    sortCode: string;
};
export default function (txId: string, csrfToken: string, bankInfo: BankInfo): Promise<nextStep>;
export {};
